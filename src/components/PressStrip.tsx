/**
 * PressStrip — DEPRECATED June 2026 audit pass.
 * Replaced by Mark.tsx (the brand-identity strip with press logos).
 * Kept as a no-op export so any stale import compiles.
 */
export default function PressStrip() {
  return null;
}
